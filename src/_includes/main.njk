<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />

        <title>{{ title }}</title>

        {% font fonts %}

        <!-- prettier-ignore -->
        {% set css %} {% include "../_assets/styles/base.css" %} {% endset %}

        <style>
            {{ css | cssmin | safe }}
        </style>
    </head>

    <body>
        {{ content | safe }}
    </body>
</html>
